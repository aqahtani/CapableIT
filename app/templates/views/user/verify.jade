extends ../../layouts/default

block intro
  .container
    h1 Verify
    
block content
  .container
    .row: .col-sm-8.col-sm-offset-2.col-md-6.col-md-offset-3
      .jumbotron
        if data.verified
          h3 Thank you for verifying your email, you can now sign in.

        if data.linked
          h3 We have also linked your account to your organization
  
        //-pre= JSON.stringify(data.dump, null, 4)
        