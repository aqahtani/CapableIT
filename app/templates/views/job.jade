extends ../layouts/default
include ../mixins/job-detail

block content
  .container
    .row: .col-sm-12
      article
        if !data.job
          h2 Invalid Job.
        else
          +jobDetail(data.job)